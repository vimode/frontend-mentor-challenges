{
  "name": "@todo-app/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "./backend/",
    "./frontend/"
  ],
  "scripts": {
    "build": "npm run build:ui --workspace @todo-app/backend",
    "start": "npm run start --workspace @todo-app/backend"
  }
}
